<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pils.disconnect">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.pils.disconnect.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="DYSCO"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.pils.disconnect.attenteconnexion"></activity>
        <activity android:name="com.pils.disconnect.Rejoindre" />
        <activity android:name="com.pils.disconnect.choix_partie" />
        <activity android:name="com.pils.disconnect.ParametresPartie"/>
        <activity android:name="com.pils.disconnect.Leave"/>
        <activity android:name="com.pils.disconnect.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.pils.disconnect.BlackScreen"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>